.export PPU_CTRL, PPU_MASK, PPU_STATUS, PPU_OAM_ADDR, PPU_OAM_DATA
.export PPU_SCROLL, PPU_ADDR, PPU_DATA, APU_DMC_FREQ, PPU_OAM_DMA
.export CTRL_PORT1, CTRL_PORT2

PPU_CTRL        = $2000
PPU_MASK        = $2001
PPU_STATUS      = $2002
PPU_OAM_ADDR    = $2003
PPU_OAM_DATA    = $2004
PPU_SCROLL      = $2005
PPU_ADDR	    = $2006
PPU_DATA	    = $2007
APU_DMC_FREQ    = $4010
PPU_OAM_DMA     = $4014
CTRL_PORT1		= $4016
CTRL_PORT2		= $4017
